# FULL-SLIM: text + font + everyday UI pictures only.
# Skips the long tail (race cards, gacha plates, comics stay stock).
# (assets/atlas and assets/movies are never indexed: stat-number bug + size.)
assets/an_texture_sets
assets/textures/single
assets/textures/home
assets/textures/chara
assets/textures/challengematch
assets/textures/transferevent
assets/textures/uianimation
